export interface CarAccessoryModel {
  id: string;
  price: number;
  title: string;
  photos: string[];
  quantity: string;
  outerType: string;
  innerType: string;
  available: boolean;
  description: string;
}
